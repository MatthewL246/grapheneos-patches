diff --git a/generate-release.sh b/generate-release.sh
index 049b0cd..16ce1e8 100755
--- a/generate-release.sh
+++ b/generate-release.sh
@@ -174,6 +174,9 @@ ota_from_target_files -k "$KEY_DIR/releasekey" $TARGET_FILES \
     $DEVICE-ota_update-$BUILD_NUMBER.zip
 script/generate-metadata $DEVICE-ota_update-$BUILD_NUMBER.zip
 
+# After installing for the first time and relocking the bootloader, we only need the OTA update, not the factory images or install zip
+exit
+
 img_from_target_files $TARGET_FILES $DEVICE-img-$BUILD_NUMBER.zip
 
 source device/common/generate-factory-images-common.sh
